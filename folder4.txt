// Exploring the shell and the server

// for help
mongod --help

// setting dbpath and logpath
mongod --dbpath c:users/path/db
mongod --dbpath c:users/path/db --logpath c:users/path/logs/logFile.log


// MongoDB as background service
fork command is not available in Windows 
step:1. close everything and open cmd with administrive privilages
step:2. net start MongoDB
step:3 to close the service 
        use admin
        db.shutdownServer()
        or 
        open cmd with admin privilages and 
        net stop MongoDB