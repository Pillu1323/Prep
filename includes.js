// It operates on simple form of array like array of numbers
// it retruns true or false , hence needs to store in a variable

const a = [1, 2, 3, 4, 5, 6];
const isTwoIncluded = a.includes(2);
console.log(isTwoIncluded); // returns true;
a.indexOf();
